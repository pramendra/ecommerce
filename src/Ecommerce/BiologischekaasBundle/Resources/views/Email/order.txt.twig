Beste {{ client.name }},

Bedankt voor je bestelling bij AddictedToVintage. Hieronder vindt u de gegevens met betrekking tot uw bestelling.

Ordernummer: {{ order.orderNr }}
Betaalmethode: {{ order.paymethod.title }}

{% if order.paymethod.id == 13 %}

Let op: Je bestelling wordt pas verzonden wanneer deze is betaald onder vermelding van je ordernummer {{ order.orderNr }}. 

Betaalgegevens:
    
Bankrekening: 57.02.31.426
Ter name van: A.Tichelaar te Leeuwarden 
IBAN: NL50ABNA0570231426
BIC: ABNANL2A
Bank:ABN AMRO

{% endif %}


Mijn bestelling

    {% for item in order.products %}

    {% set productTotal = item.amount * item.price %}

    - {{ item.amount }} x {{ item.product.name }}    &euro; {{ productTotal|number_format(2, ',', '.') }}

    {% endfor %}

Verzendkosten: &euro; {{ order.shipping.costs|number_format(2, ',', '.') }}
Totaalbedrag:&euro; {{ order.total|number_format(2, ',', '.') }}

Met vriendelijke groet, 
AddictedToVintage.nl